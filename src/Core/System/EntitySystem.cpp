#include "./EntitySystem.h"

// void EntitySystem::unregisterComponents(Identity::Type entityId) {
//     auto e = m_EntityContext->getEntity(entityId);
//     //TODO: Use entity to unregister properly
//     for (const auto &reg: m_registries) {
//         reg->unregisterComponents(entityId);
//     }
// }